import type { BlockContainerProps } from "@/lib/canvas/block-spec.types.ts";

/** Structural row container — rendered only inside {@link TableView}. */
export function TableRowView(_props: BlockContainerProps) {
  return null;
}
