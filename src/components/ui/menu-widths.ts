/**
 * Standard width for action-heavy menus. The 16.5rem target is only slightly
 * wider than the previous 16rem page/database menus, while the available-width
 * cap lets Base UI shrink it near viewport edges.
 */
export const standardActionMenuWidthClassName =
  "w-[16.5rem] min-w-0 max-w-(--available-width)";
