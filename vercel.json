{
  "ignoreCommand": "bash -c 'echo \"$VERCEL_GIT_COMMIT_MESSAGE\" | grep -Fq \"[skip deploy]\" && exit 0 || exit 1'",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter web build && rm -rf .vercel/output && mkdir -p .vercel && cp -r apps/web/.vercel/output .vercel/output"
}
